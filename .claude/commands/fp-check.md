FP確認待ち事項を洗い出します。

以下の手順で進めてください：

1. `src/` 以下のすべての Python ファイルから `TODO: FPに確認` コメントを検索する
2. `tests/` 以下から `@pytest.mark.xfail` かつ `FP` を含むものを検索する
3. `docs/spec/01_requirements.md` § 8 の未確定事項一覧と照合する

4. 結果を以下の形式で報告する：

   **コード内の未確認事項:**
   | ファイル | 行 | 内容 |
   | -------- | -- | ---- |
   | ...      | .. | ...  |

   **要件定義書の未確定事項（§ 8）:**
   - （一覧）

   **不一致・漏れ:**
   - コードに TODO があるが § 8 にない → 追記が必要
   - § 8 にあるがコードに TODO がない → 実装済みか未着手か確認が必要

5. 「FP確認が必要な事項を整理しました。確認を取りますか？」と問いかける
